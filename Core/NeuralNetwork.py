from Perceptron import Perceptron
import numpy as np
import matplotlib.pyplot as plt

class NeuralNetwork:
    def __init__(self, estimators, input) -> None:
        pass
    
    def train(self, x, y, epochs=50, lr=0.1) -> None:
        
        pass

